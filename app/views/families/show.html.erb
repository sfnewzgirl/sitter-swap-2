<div class="container-fluid">
  <div class="row welcome-header clearfix">
    <div class="col-lg-6 col-sm-12" id="sitter-swap-logo">
      <h1>SitterSwap</h1>
    </div>
    <div class="col-lg-6 col-sm-12" id="welcome-header-links">
      <%= link_to 'Log Out', logout_path %>
      <%= link_to 'About', about_path %>
      <%= link_to 'Home', root_path %>
      <%= link_to 'Profile', person_path(current_person) %>
    </div>
  </div>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-lg-12">
      <div class="col-lg-6">
        <center>
          <div class="log-in-header">
            <h1>Family Details</h1>
            <p id="notice"><%= notice %></p>
          </div>
          <p>
            <strong>Family name:</strong>
            <%= @family.family_name %>
          </p>
          <div id="profile-page-link">
            <%= link_to 'Profile', person_path(current_person) %>
          </div>
        </center>
      </div>
      <div class="col-lg-6">
        <center>
          <div class="log-in-header">
            <h1>Relationships</h1>
            <p id="notice"><%= notice %></p>
          </div>
          <% @family.people.each do |person| %>
            <p><strong>Name: </strong><%= link_to person.first_name + " " + person.last_name, '/people/' + person.id.to_s %></p>
          <% end %>
        </center>
      </div>
    </div>
  </div>
</div>
